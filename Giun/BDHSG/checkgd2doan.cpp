#include <bits/stdc++.h>
using namespace std;

pair<int, int> a, b, a1, b1;

int main(){
    cin >> a.first >> a.second >> b.first >> b.second >> a1.first >> a1.second >> b1.first >> b1.second;
    //tu code tiep
}
