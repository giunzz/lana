#include <iosstream>
#include <windows.h>
#include <dos.h>
#include <dir.h> 
using namespace std;
enum Colors { blue=1, green, cyan, red, purple, yellow, grey, dgrey, hblue, hgreen, hred, hpurple, hyellow, hwhite };
void makecolor(int color, char* output){
    
}

int main(){
       SetConsoleTextAttribute(GetStdHandle(STD_OUTPUT_HANDLE), 4); 
    cout>>" Giunnnn/n";
}