// /*Hoàng Thái Thụy*/ //
#include <bits/stdc++.h>
#define nmax 100005
#define task "SEQUENCE"
#define U first
#define V second
using namespace std;
typedef pair<int,int> pii;
typedef vector<int> vct;
int main(){
    freopen(task".inp","r",stdin);
    freopen(task".out","w",stdout);
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int n, m , k,a[nmax], b[nmax];
    cin >>n >> m >> k;
    for (int i = 1; i <= m; i++) {cin >> a[i];cout << a[i] << ' ';}
    for (int i = 1; i <= n; i++) {cin >> b[i]; cout << b[i] << ' ';}

}
