#include <bits/stdc++.h>
using namespace std;

int n, u, x, y;

main(int argc, char const *argv[])
{
    freopen("ROBOTS.inp", "r", stdin);
    freopen("ROBOTS.out", "w", stdout);
    cin >> n >> u;
    while(n--)  cin >> x >> y;
    cin >> x >> y;
    cout << rand();
    // cerr << rand();
    return 0;
}
