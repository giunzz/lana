#include <bits/stdc++.h>

using namespace std;
const int maxn = 1e7;
int n, p, a[maxn];

int find(int k){
    
}

int main(){
    freopen("test.INP", "r", stdin);
    freopen("test.OUT", "w", stdout);
    cin >> n >> p;
    for (int i = 1; i <= n; i++) cin >> a[i];
    cout << find(p);
    return 0;
}