#include <bits/stdc++.h>

using namespace std;
vector <int> b;
stack <int> s;

int main(){
    freopen("test.inp", "r", stdin);
    freopen("test.out", "w", stdout);
    int a;
    b.push_back(3);
    s.push(3);
    cin >> a;
    if (a == 2) cout << 3;
    cout << a;
}