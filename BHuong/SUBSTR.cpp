#include <bits/stdc++.h>
#define ll long long
#define giuncute ios_base::sync_with_stdio(0),cin.tie(0);
using namespace std;
string s1 ;
int dd[300]={0};
int main()
{
    giuncute;
    freopen("giun.inp","r",stdin);
    freopen("g.out","w",stdout);
    getline(cin , s1) ;
    int i = 0 , j = 1;
    while (j != s1.length())
    {
        dd[(int)s1[i]]++;
        if (s1[i] !)
    }
