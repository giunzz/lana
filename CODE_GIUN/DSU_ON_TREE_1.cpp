//Given a tree and weights of nodes. Weights are non-negative integers. Task is to find maximum size of a subtree of a given tree such that all nodes are even in weights.
#include <bits/stdc++.h>
using namespace std;
#define giuncute ios_base::sync_with_stdio(0) , cin.tie(0);
#define ll long long 
const ll maxn = 1e6+7;
ll p[maxn]  , m ,n , size[maxn] , color[maxn] , u , v ;
vector < ll > G[maxn];

int main()
{
    giuncute;
    freopen("giun.inp","r",stdin);
    freopen("giun.out","w",stdout);
    cin >> n >> m ; 
    for (int i = 1 ; i <= m ; i++) 
    {
        cin >> u >> v;
        G[u]
    }
}