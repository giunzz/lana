#include <bits/stdc++.h>
using namespace std;
long long n;
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    freopen("FUNGAME.INP", "r", stdin);
    freopen("FUNGAME.OUT", "w", stdout);
    cin>>n;
    if (n % 2==0) cout << 0;
    else cout << 1;
  return 0;
}

