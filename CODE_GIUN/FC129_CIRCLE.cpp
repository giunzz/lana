#include <bits/stdc++.h>
#define ll long long
#define giuncute ios_base::sync_with_stdio(0) , cin.tie(0);
#define pl pair<ll,ll>
#define fi first
#define se second
const ll MOD = 1e9 + 7;
const ll maxn = 4e5 + 7;
using namespace std;

ll n , x[maxn] , r[maxn];

int main()
{
    giuncute;
    cin >> n ;
    for (int i = 1 ; i <= n ; i++)
    {
        cin >> x[i] >> r[i];
    }
    cout << n+1;
}