#include<bits/stdc++.h>
using namespace std;
string a;
vector<string> st;
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    freopen("giun.inp","r",stdin);freopen("giun.out","w",stdout);
    while (cin >> a) {st.push_back(a);}
    for (long long i = 0 ; i < (long long ) st.size() - 1; i++) 
    {
        
    }
    
        
        (long long   i = (long long ) st.size() -1 ; i >= 0 ; i--) cout<<st[i]; 
    return 0;
}