/*#include <bits/stdc++.h>
#define ll long long
#define giuncute ios_base::sync_with_stdio(0),cin.tie(0);
using namespace std;
int n ;

ll pow (int n)
{
    if (n == 0) return 1;
    ll baby = pow(n/2);
    ll ans = baby*baby;
    if (n%2) ans *=2;
    return ans;
}
int main()

{
    giuncute;
    //freopen("giun.inp","r",stdin);
    //freopen("g.out","w",stdout);
    cin >> n;
    cout << pow(n);
}*/

