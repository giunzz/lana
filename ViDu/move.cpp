#include<bits/stdc++.h>
using namespace std;
int n, x[(int) 5e3 + 7], y[[(int) 5e3 + 7],k;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    freopen("giun.inp","r",stdin);
    freopen("giun.out","w",stdout);
    cin >> n >> k;
    for (int i = 1 ; i <= n ; i++) cin >> x[i] >> y[i];
    for (int i = 2 ; i < n - 1 ; i++)
    {
        for (int j = i+1 ; j < n ; j++)
        if (abs|)
    }
}