#include <iostream>
#include <string>
#include <vector>
#include <algorithm>

using namespace std;
int ma = 0;
int main()
{
    int n;
    cin >> n; 
    for (int i = 0; i < n; i++) {
        int v;
        cin >> v; 
		ma = max (ma,v);
    }
    for (int i = ma ; i >= 1 ; i-- )
	{
		in = '';
		for (int j = 1 ; j <= v ; )
	}
}