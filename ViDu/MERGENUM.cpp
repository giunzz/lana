#include <bits/stdc++.h>
using namespace std;
string s;
int t;

void sol(){
    int a, b, c, d;
}

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    freopen("MERGENUM.INP", "r", stdin);
    freopen("MERGENUM.OUT", "w", stdout);
    cin>>a;

    return 0;
}
