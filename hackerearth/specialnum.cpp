#include <bits/stdc++.h>
using namespace std;
long long l, r, t, dt[(int)1e7 + 7];

void setdata(){
    int i = 1, num = 0, digit;
    vector<int> st; 
    while (num <= 2222222222){

    }
}

void sol(long long l, long long r){
    for (long long i = l; i <= r; i++){

    }
}

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    freopen("specialnum.inp", "r", stdin);
    freopen("specialnum.out", "w", stdout);
    cin >> t;
    for (int i = 1; i <= t; i++){
        cin >> l >> r;
        sol(l, r);
    }
}